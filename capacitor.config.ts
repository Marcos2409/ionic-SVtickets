import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.fullstackpro.ionic.SVtickets',
  appName: 'ionic-SVtickets',
  webDir: 'www',
  android: {
    allowMixedContent: true
  }
};

export default config;
